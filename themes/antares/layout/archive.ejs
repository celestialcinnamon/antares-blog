<%- /*partial('_partial/header', {title: 'Post Archives'})*/ %>
<main class="main card-grid">
    <% page.posts.each(function(item){ %>
        <%- partial('_partial/archive', {item: item}) %>
    <% }); %>
</main>

